version: 2

sources:
  - name: quip
    database: alloy-prod-customer-exports
    description: |
      This source lives in the vendor's data warehouse. We currently do not store the raw data into our own datawarehouse. Instead, we query the vendor's data warehouse directly. Only certain entities have access to this source, to configure access, check the link below.
    loader: Direct #https://app.alloy.ai/quip/data/destinations/bigquery/39/configuration
    tables:
      - name: data
      - name: location
      - name: product